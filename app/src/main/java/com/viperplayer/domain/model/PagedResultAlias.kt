package com.viperplayer.domain.model

import com.viperplayer.plugin.PagedResult as PluginPagedResult

/**
 * Alias for PagedResult from plugin-sdk.
 * Used as the domain model for paginated results.
 */
typealias PagedResult<T> = PluginPagedResult<T>

