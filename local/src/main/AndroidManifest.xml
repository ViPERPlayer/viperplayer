<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Storage permissions for accessing local audio files -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!--
        This module is embedded in the host app: the manifest below merges into the app manifest,
        so the plugin ships built in and the host discovers/binds its own (in-process) service the
        same way it does external plugin packages. Nothing needs to be exported.
    -->
    <application>
        <activity
            android:name="com.viperplayer.local.LocalSettingsActivity"
            android:exported="false" />

        <service
            android:name="com.viperplayer.local.LocalService"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.viperplayer.plugin.ViperPluginService" />
            </intent-filter>
            <meta-data
                android:name="com.viperplayer.plugin.NAME"
                android:value="Local Files" />
        </service>
    </application>

</manifest>
